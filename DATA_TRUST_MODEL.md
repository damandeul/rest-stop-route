# 쉬어갈지도 — 데이터 신뢰도·운영 모델

- 문서 상태: Phase 1 내부 운영 기준안
- 작성일: 2026-07-27 (KST)
- 근거: `PHASE_1_BRIEF.md`, `MVP_SPEC.md`, `DATA_AND_NAME_AUDIT.md`
- 적용 범위: 장소 후보, 지역·조건별 커버리지, 수집·갱신·정정 운영
- 주의: 이 문서의 신뢰도는 **자료의 출처·최신성·완전성**에 관한 표시이며 장소의 안전, 실제 개방, 냉방 작동, 좌석 또는 급수를 보장하는 점수가 아니다.

## 1. 결정 요약

1. 장소 신뢰도는 `출처`, `갱신 상태`, `운영시간·접근조건`, `지역·조건 커버리지`, `독립 교차확인 수`의 5축으로 계산한다.
2. 각 축은 0~2점인 내부 운영값으로 기록하되, 사용자에게는 `높음/보통/낮음`과 구체적인 이유만 보여준다. 합계만 단독 노출하지 않는다.
3. 좌표 없음, 조건 미확인, 이용 제한 미확인 같은 **하드 게이트**를 점수보다 먼저 적용한다. 점수가 높아도 선택 조건 충족 근거가 없으면 경로 연결점으로 쓰지 않는다.
4. 자료가 없는 지역은 `시설 없음`이 아니라 `정보 없음/일부 자료/수집 실패/만료`로 원인을 구분한다.
5. 만료 기준은 임의의 절대 날짜 하나가 아니라 원출처의 공표 갱신주기, 행별 기준일, 수집 성공 시각, 계절 유효기간을 함께 사용한다.
6. 운영비는 금액을 추정하지 않는다. API·지오코딩·저장·모니터링·수동 검수별 실제 청구액과 실제 투입시간을 기록한 뒤 확대 여부를 판단한다.

## 2. 먼저 적용하는 하드 게이트

점수 계산 전에 아래 규칙을 적용한다.

| 조건 | 경로 연결점 사용 | 지도·상세 노출 | 표시 |
|---|---|---|---|
| 위·경도 없음 | 불가 | 주소 후보로만 가능 | `좌표 정보 없음` |
| 주소 지오코딩 좌표이나 정확도 미상 | 기본 불가 | 가능 | `위치 정확도 미확인` |
| 선택 조건이 `unknown` | 해당 조건 충족점으로 사용 불가 | 가능 | `○○ 여부 미확인` |
| 선택 조건이 `false` | 해당 조건 충족점으로 사용 불가 | 가능 | `○○ 없음` |
| 예약·유료·관계자 전용 또는 접근조건 미상인 공공시설 개방정보 | 자동 연결 불가 | 수동 확인 후 후보 노출 | `바로 이용 가능한지 확인 필요` |
| 폐쇄·철거·운영종료가 공식 확인됨 | 불가 | 정정 이력에서만 보존 | `운영 종료` |
| 원출처·원문 링크·원본 식별자 중 핵심 추적정보 없음 | 불가 | 내부 검수만 | `출처 확인 불가` |
| 이용허락 또는 파생 좌표 재배포 조건 미확인 | 외부 배포 불가 | 내부 프로토타입만 | `이용조건 확인 중` |
| 데이터 만료 | 긍정 경로 연결점으로 사용 불가 | 경고와 함께 과거 정보로만 가능 | `마지막 확인이 오래됨` |

조건 값은 반드시 `true / false / unknown` 3값을 쓴다. 결측을 `false`로 변환하지 않는다.

## 3. 장소별 5축 신뢰도 산식

### 3.1 점수 정의

| 축 | 2점 | 1점 | 0점 |
|---|---|---|---|
| 출처 `P` | 공식 원문을 직접 수집했고 원본 ID·URL·제공기관·이용조건을 추적 가능 | 공식 자료를 주소 지오코딩·필드 파싱 등으로 변환했으며 변환 이력 추적 가능, 또는 승인된 현장 확인 | 비공식 2차 출처만 있거나 원문·이용조건·변환 이력을 추적할 수 없음 |
| 갱신 `F` | 수집 파이프라인이 정상이고 원출처 공표 주기 안이며 행 기준일/수정일 또는 확인일이 유효 | 원출처는 접근 가능하나 공표 주기를 넘겼거나 행 기준일이 오래되어 재확인 필요 | 만료, 날짜 없음, 수집 실패가 해소되지 않음 |
| 운영 `O` | 평일·주말·휴일 시간과 접근 제한을 구조화했고 현재 질의 시각에 대한 판정 근거가 있음 | 운영시간 또는 휴무·접근조건 중 일부만 있음 | 운영시간과 접근조건이 모두 없거나 상충함 |
| 커버리지 `C` | 해당 지역×조건의 수집 대상과 제공기관 범위가 정의되고 현 수집분의 누락 여부를 판별 가능 | 일부 공식 자료는 있으나 제공기관 또는 시설 범위가 불완전함 | 지역×조건 소스가 없거나 범위를 판별할 수 없음 |
| 교차확인 `X` | 서로 독립적인 추가 근거 2개 이상이 핵심 사실을 일치 확인 | 독립적인 추가 근거 1개가 일치 확인 | 추가 근거가 없거나 근거 간 충돌이 미해결 |

`교차확인 수`는 원본을 복제한 미러·재배포 페이지를 별도 근거로 세지 않는다. 같은 제공기관의 API와 CSV가 동일 원본이면 1개 출처다. 교차확인 대상은 좌표, 장소 존재, 선택 조건, 운영정보이며 무엇을 확인했는지 `crosscheck_fields`에 저장한다.

### 3.2 등급 매핑

- 내부 합계: `T = P + F + O + C + X` (0~10)
- `높음`: 9~10점이며 하드 게이트 통과
- `보통`: 6~8점이며 하드 게이트 통과
- `낮음`: 0~5점, 또는 아래 강등 규칙 적용

이 구간은 Phase 1의 **운영 가설**이다. 실제 오안내율과 현장 일치율을 얻은 뒤 조정하며, 점수 구간을 데이터 품질의 객관적 확률로 표현하지 않는다.

강등 규칙:

1. `P=0`, `F=0`, `C=0` 중 하나라도 있으면 최종 등급은 최대 `낮음`이다.
2. `O=0`이면 현재 운영 중이라는 표현을 금지하고 최종 등급은 최대 `낮음`이다.
3. 주소 지오코딩 좌표는 정확도가 검증되기 전 최대 `낮음`, 건물/필지 수준 정확도가 확인되면 최대 `보통`이다.
4. 공원 중심점은 출입구·벤치 위치가 아니므로 경로 연결 좌표로 쓰지 않으며, 공원 후보 자체의 신뢰도와 별개로 `coordinate_use=centroid_only`를 표시한다.
5. 근거 충돌이 있으면 최신 공식 정정 전까지 `X=0`, `conflict_open=true`로 두고 긍정 경로 연결에서 제외한다.

### 3.3 사용자 표시 예

숫자만 보여주지 않고 이유를 조합한다.

- `높음 · 공식 자료 · 운영시간 확인 · 최근 수집 · 추가 근거 2개`
- `보통 · 공식 위치 확인 · 주말 운영시간 없음 · 지역 자료 일부`
- `낮음 · 공원 중심점 · 벤치 위치 미상 · 마지막 확인이 오래됨`

금지 문구: `안전도 90점`, `운영 확률 80%`, `믿고 이용 가능`. 이 모델은 그러한 확률을 산출하지 않는다.

## 4. 갱신·만료 규칙

서로 다른 날짜를 한 필드로 합치지 않는다.

- `source_checked_at`: 원출처 페이지·명세·이용조건을 마지막 확인한 시각
- `source_updated_at`: 데이터셋 전체의 공표 수정/갱신 시각
- `record_updated_at`: 개별 행의 수정시각 또는 데이터기준일
- `ingested_at`: 쉬어갈지도 시스템이 해당 버전을 수집한 시각
- `last_verified_at`: 운영자가 원문·전화·현장 등 승인된 방식으로 마지막 확인한 시각
- `valid_from`, `valid_until`: 계절 설치·한시 운영처럼 명시적 유효기간이 있을 때만 저장

### 4.1 상태 판정

1. 원출처가 공표한 갱신주기를 `declared_refresh_interval`로 저장한다.
2. 예정 수집 시각을 넘긴 첫 실패는 `수집 지연`, 연속 실패로 최신 버전을 확보하지 못하면 `수집 실패`로 표시한다. 실패 횟수는 실제 실행 로그값을 쓴다.
3. `valid_until`이 지났거나 공식 폐쇄·철거가 확인되면 즉시 `만료`다.
4. 공표 주기를 넘긴 데이터는 `재확인 필요`로 강등한다. 공표 주기의 두 배를 지나도록 새 버전·행 기준일·승인된 확인 중 어느 것도 없으면 `만료`로 처리하는 것을 Phase 1 기본 정책으로 한다.
5. 갱신주기가 `수시/실시간`처럼 불명확하거나 날짜가 없으면 자동으로 최신이라고 보지 않는다. 운영자가 실제 배포 패턴을 관찰해 주기를 확정하기 전까지 `낮음`이다.
6. 데이터셋이 매일 재배포되지만 개별 시설 값이 오래 바뀌지 않을 수 있으므로 `ingested_at`만으로 장소가 최신이라고 판단하지 않는다. 행 기준일 또는 별도 확인일을 함께 표시한다.
7. 계절형 그늘막은 설치·접힘 상태가 없으면 데이터가 최신이어도 `현재 그늘 제공`을 단정하지 않는다.

### 4.2 현재 확인된 소스별 운영 기준

| 소스 | 확인된 공표 주기·특성 | 초기 수집 정책 | 만료·주의 |
|---|---|---|---|
| 행정안전부 무더위쉼터 API | 1일 갱신 표시, 행별 `MODF_TIME`, 이용신청·serviceKey 필요 | 매일 수집 후보. API 전체 수집 후 실제 배포 패턴 확정 | 수집 최신성과 시설 당일 개방은 별개. 운영시간·냉방 결측 프로파일링 필요 |
| 전국그늘막쉼터표준데이터 | 연간, 전국 병합은 매월 초, 행별 기준일 | 월별 병합 변경 감시, 행 기준일 분포 기록 | 계절 설치·강풍/우천 접힘·고장 상태 없음. 현재 그늘 단정 금지 |
| 전국도시공원정보표준데이터 | 연간, 전국 병합은 매월 초 | 월별 병합 변경 감시, 행 기준일 기록 | 중심점은 출입구가 아님. 벤치·그늘·운영시간 미보장 |
| 전국공중화장실 CSV | 매일·2일 지연 표기, 2025-02부터 좌표 제공 중단 | 파일 버전과 파일명 날짜의 일치 확인 후 수집 | 주소 지오코딩 전 경로 사용 불가. 지오코더 이용조건·정확도 필요 |
| 전국공공시설개방정보 | 분기 | 분기 변경 감시, 후보별 수동 접근조건 검수 | 냉방 필드 없음, 유료·예약 시설 혼재. 자동 휴식점 채택 금지 |
| 지역별 음수대·벤치 | 파일별 상이, 전국 표준 골격 미확인 | 지역별 레지스트리에서 주기·필드·이용조건 개별 관리 | 자료 없는 지역은 `물/벤치 정보 없음`; 전국 조건 충족을 단정하지 않음 |

무더위쉼터 API는 2026-07-27 전량 60,672건을 두 차례 성공적으로 수집했다. 공식 좌표는 100% 유효했고 평일 시작·종료시간 완비는 17.92%, 냉방기 수량 `unknown`은 4.63%였다. 원본·정규화 데이터와 상세 결측률은 `reports/heat-shelter-profile.md`에서 추적한다. 다른 소스는 아직 전량 프로파일링 전이다.

## 5. 지역×조건 커버리지 모델

### 5.1 단위

기본 집계 단위는 `시도 → 시군구 → 조건(그늘/벤치/냉방 실내/물/화장실)`이다. 전국 검색 가능 여부와 휴식점 자료 커버리지를 별도 필드로 둔다.

필수 집계값:

- `region_code`, `condition`
- `source_count`: 독립 원출처 수
- `provider_expected_count`: 원출처가 명시한 대상 제공기관 수. 알 수 없으면 `null`
- `provider_received_count`: 실제 최신 자료를 받은 제공기관 수
- `record_total`, `record_with_usable_coordinate`, `record_not_expired`
- `record_condition_true`, `record_condition_false`, `record_condition_unknown`
- `oldest_record_date`, `newest_record_date`, `last_successful_ingestion_at`
- `coverage_status`, `coverage_reason`

### 5.2 상태와 사용자 문구

| 상태 | 판정 | 사용자 문구 |
|---|---|---|
| `확인 가능` | 대상 범위가 정의되고 최신 수집이 성공해 누락 여부를 판별할 수 있음 | `이 지역의 ○○ 공개자료 범위를 확인했어요.` |
| `일부 자료` | 공식 자료는 있으나 제공기관·시설·조건 범위가 불완전함 | `이 지역은 ○○ 자료가 일부만 제공돼요.` |
| `정보 없음` | 사용할 수 있는 원출처를 확인하지 못함 | `이 지역의 ○○ 공개자료를 확인하지 못했어요. ○○ 시설이 없다는 뜻은 아니에요.` |
| `수집 실패` | 알려진 원출처가 있으나 최신 수집에 실패함 | `○○ 자료를 지금 불러오지 못했어요. 시설 유무를 판단할 수 없어요.` |
| `만료` | 확보 자료가 모두 만료되어 현재 판단에 쓸 수 없음 | `○○ 자료의 마지막 확인이 오래돼 현재 상태를 판단할 수 없어요.` |
| `조건 미제공` | 데이터셋은 있으나 해당 조건 필드가 없음 | `장소 자료는 있지만 ○○ 여부는 제공되지 않아요.` |

`시설 없음`은 현재 유효한 공식 원출처가 해당 지역·조건을 완전하게 조사했고 0건임을 명시할 때만 사용할 수 있다. 현재 감사 결과만으로는 음수대·벤치가 없는 지역을 판정할 수 없다.

### 5.3 결과 상태 우선순위

1. 지역 또는 선택 조건 중 하나라도 `정보 없음/수집 실패/만료/조건 미제공`이고 그 누락이 연결 판단을 바꿀 수 있으면 전체 결과는 `정보 부족`이다.
2. 정보 범위가 확인된 데이터만으로 후보 간 구간이 사용자 한도를 넘을 때 `휴식점 부족`이다.
3. 둘이 함께 발생하면 상단 주 상태는 `정보 부족`, 계산 가능한 구간의 초과는 하위 경고로 병기한다.
4. 경로 서비스가 시간을 만들지 못하면 `경로 계산 불가`이며 거리나 시간을 임의 추정하지 않는다.

## 6. 통합 레코드와 감사 로그

장소 레코드 최소 필드:

```text
place_id, source_id, original_id, name, category
source_provider, source_url, source_license, source_checked_at
lat, lon, coordinate_source, coordinate_precision, coordinate_use
road_address, lot_address
weekday_hours, weekend_hours, holiday_hours, closed_days
access_restriction
has_aircon, has_shade, has_bench, has_water, has_toilet
source_updated_at, record_updated_at, ingested_at, last_verified_at
valid_from, valid_until
provenance_score, freshness_score, operation_score, coverage_score, crosscheck_score
confidence_level, confidence_reasons
crosscheck_count, crosscheck_fields, conflict_open
record_status, warning
```

감사 가능성을 위해 원본 스냅샷의 체크섬, 수집 작업 ID, 파서 버전, 변경 전후값을 보존한다. 장소 값은 조용히 덮어쓰지 않고 변경 이력을 남긴다. 원본에 개인정보가 포함되면 필요한 필드만 처리하며 이 문서·Kanban에는 원본 개인정보를 남기지 않는다.

## 7. 정정·충돌·삭제 운영

### 7.1 상태 흐름

`active → needs_review → corrected / suppressed / removed → active(재확인 시)`

- `needs_review`: 원출처 간 충돌, 갑작스러운 좌표 이동, 운영시간 형식 오류, 공식 정정 발견
- `corrected`: 공식 새 버전 또는 승인된 검수로 값 변경
- `suppressed`: 사실 여부를 확정하지 못해 사용자 노출과 경로 연결을 임시 중단
- `removed`: 공식 폐쇄·철거·중복 레코드 확인

### 7.2 우선순위

1. 위치가 다른 시설로 이동한 것처럼 보이는 좌표 변경
2. 폐쇄·철거·접근 제한·운영시간 충돌
3. 냉방·그늘·물·화장실·벤치 조건 충돌
4. 이름·주소 표기 차이

1~3은 해결 전 긍정 경로 연결에서 제외한다. 사용자 제보 기능은 첫 MVP 범위가 아니므로 공개 제보 접수 체계를 가정하지 않는다. 대표가 별도 승인한 원문 확인·현장 검수 결과만 `verification_method`와 증거 시각을 기록해 반영한다.

## 8. 운영 작업과 부담 기록

### 8.1 필수 작업

| 작업 | 자동화 가능 부분 | 사람 확인이 필요한 부분 |
|---|---|---|
| 소스 레지스트리 | URL 상태, 공표 주기, 최신 버전 감지 | 이용조건 변경, 제공 범위 해석 |
| 수집 | API/CSV 다운로드, 재시도, 원본 체크섬 | 인증 만료·스키마 변경 해결 |
| 정규화 | 필드 매핑, 3값 변환, 날짜 파싱 | 자유서술 시설·운영시간 해석 예외 |
| 품질검사 | 좌표 범위, 중복, 결측, 급격한 건수 변화 | 실제 이상인지 원출처 오류인지 판단 |
| 지오코딩 | 승인된 제공자 호출과 정확도 저장 | 애매한 주소 검수, 라이선스 검토 |
| 교차확인 | ID·주소·좌표 근접 후보 생성 | 서로 다른 시설/동일 시설 판정 |
| 만료·정정 | 규칙 기반 강등·노출 중단 | 충돌 해결과 복구 승인 |
| 지역 커버리지 | 지역×조건 집계 | `완전/일부` 판정 근거 검토 |

### 8.2 비용·시간 원장

임의 비용을 만들지 않고 실행마다 아래 실제값을 기록한다.

- `run_date`, `source_id`, `records_read`, `records_changed`, `run_status`
- API·지오코딩·저장·모니터링 `actual_cost_krw`와 영수증/청구 근거
- 자동 실행시간, 수동 검수시간, 장애 대응시간
- 정정 건수, 미해결 충돌 건수, 만료 레코드 수
- 경로 연결에 실제 사용된 레코드 수와 사용되지 않은 수집 레코드 수

비용을 아직 지출하지 않았거나 청구액을 확인하지 못했으면 `0`으로 꾸미지 않고 `미발생` 또는 `미확인`으로 구분한다. API 신청, 유료 지오코더, 저장·모니터링 지출은 대표 승인 전 실행하지 않는다.

### 8.3 부담이 큰 항목

- 무더위쉼터 API 키의 안전한 보관·인증 만료 대응과 일별 수집 감시
- 화장실 52,255행 주소 지오코딩의 이용조건·정확도·실제 비용
- 지역별 음수대·벤치 파일의 서로 다른 스키마와 갱신주기 관리
- 그늘막 계절 상태 및 공원 중심점/출입구 오차의 수동 확인
- 공공시설의 유료·예약·냉방·즉시 출입 가능 여부 수동 판정

따라서 전국 수집량이 많다는 이유만으로 채택하지 않고, 실제 경로 판단에 기여한 장소 수 대비 실제 운영시간·비용을 함께 본다.

## 9. Phase 1 검증과 의사결정 기준

### 9.1 먼저 수집할 실제값

1. 무더위쉼터 API와 그늘막·도시공원 파일의 현행 광역단위별 행 수, 좌표·운영시간·조건 필드 결측률, 기준일 분포. 무더위쉼터 API는 전남광주통합특별시를 포함한 16개 광역코드를 사용하므로 다른 자료의 과거 17개 시·도 코드와 변환 이력을 보존한다.
2. 지역별 음수대·벤치 자료의 실제 시군구 범위
3. 고밀도 도심·중소도시·교외 각 10개, 총 30개 표본 경로의 3/5/10분 연결 결과
4. 표본 장소의 실제 접근 가능 여부, 운영시간 일치 여부, 공원 중심점·출입구 오차, 잘못된 연결 수
5. 소스별 실제 수집 성공률, 장애·정정 건수, 수동 검수시간, 실제 비용

표본 지역 선정과 외부·현장 검수, 비용 지출은 대표 승인 후 수행한다.

### 9.2 성공 — 유지·다음 제작 단계

아래를 모두 충족할 때다.

1. 경로에 노출되는 모든 장소가 원본 ID·출처 URL·수집일·갱신 또는 확인 상태를 추적할 수 있다.
2. 선택 조건의 `unknown`을 `true`로 사용한 연결이 없고, 좌표 미확인·만료·접근제한 미확인 장소가 긍정 경로에 섞이지 않는다.
3. 현행 광역단위 각각에 대해 조건별 `확인 가능/일부 자료/정보 없음/수집 실패/만료/조건 미제공` 중 하나를 근거와 함께 생성할 수 있다.
4. 30개 표본 경로에서 실제 자료로 `후보가 이어짐/휴식점 부족/정보 부족`을 구분하고, 검수자가 같은 근거로 판정을 재현할 수 있다.
5. 두 번의 예정 수집 실행에서 스키마·중복·좌표·날짜 품질검사가 통과하고, 실패 시 이전 자료를 최신인 것처럼 표시하지 않는다.
6. 실제 운영시간과 실제 비용 원장이 작성되어 다음 단계 예산·인력 판단이 가능하다.

### 9.3 수정 — 범위나 약속 축소 후 재검증

아래 중 하나면 기능 추가보다 데이터·문구·범위를 먼저 수정한다.

- 냉방 실내는 연결 가능하지만 물·벤치가 대부분 `unknown`이면, 전국 핵심 조건에서 빼거나 `지역별 실험 조건`으로 낮춘다.
- 공원 중심점·화장실 지오코딩 오차가 잘못된 연결을 만들면 해당 유형을 자동 경유점에서 후보 표시로 낮춘다.
- 운영시간 결측이 많으면 `운영 가능` 필터를 제거하고 상세 경고·원문 확인 중심으로 바꾼다.
- 지역별 커버리지를 판별할 수 있으나 전국 자동 운영 부담이 크면 대표 검수 지역부터 운영하고 전국은 `정보 부족` 표시만 유지한다.
- 교차확인 요구가 병목이면 등급을 높이기 위해 약한 근거를 추가하지 말고 `보통/낮음` 상태로 정직하게 노출한다.

### 9.4 중단 또는 문제 재정의

아래 중 하나면 전국 연속 휴식 경로 약속을 중단하거나 `공식 무더위쉼터 확인 지도`처럼 더 좁은 문제로 재정의한다.

1. 지역×조건 데이터 유무를 안정적으로 판별하지 못해 `정보 부족`과 `휴식점 부족`을 구분할 수 없다.
2. 긍정 경로를 만들기 위해 만료·좌표 미확인·조건 `unknown` 장소를 반복적으로 사용해야 한다.
3. 표본에서 공원 중심점·지오코딩·운영정보 오류가 사용자의 이동 판단을 반복적으로 잘못 유도하며 자동 제외 규칙으로 해결되지 않는다.
4. 핵심 소스의 이용조건이 파생 데이터 저장·앱 노출을 허용하지 않거나 필요한 승인·API 접근을 확보하지 못한다.
5. 실제 운영비·수동 검수시간이 대표가 승인한 한도를 넘고, 조건 또는 지역 축소로도 지속 가능하지 않다. 승인 한도는 실제 원장을 본 뒤 대표가 정하며 이 문서에서 임의 금액을 만들지 않는다.
6. 전국 표본에서 `높음/보통` 등급 연결점만으로 5분 연속보행 가치를 검증할 경로를 만들지 못해 핵심 JTBD를 시험할 수 없다.

## 10. 소스별 현재 초기 판정

전량 수집 전의 구조적 판정이며 실제 장소 등급이 아니다.

| 소스 | 초기 역할 | 구조적 신뢰 강점 | 제한 |
|---|---|---|---|
| 무더위쉼터 API | 냉방 실내 1차 연결점 | 공식, 좌표·운영시간·냉방·수정시간 필드 | 키 필요, 행별 결측·당일 개방 미검증 |
| 그늘막 표준 | 그늘 후보 | 공식, 좌표, 행별 기준일 | 86개 제공기관, 운영·계절 상태·좌석 없음 |
| 도시공원 표준 | 공원 후보 | 공식, 좌표, 234개 제공기관 | 중심점 오차, 그늘·벤치·물·운영시간 미보장 |
| 공중화장실 CSV | 주소 후보 | 공식, 운영시간·시설 필드 | 현재 좌표 없음, 지오코딩 필요 |
| 공공시설 개방정보 | 수동 검증 후보 | 공식, 좌표·운영시간·요금·신청 필드 | 냉방 없음, 예약·유료 혼재, 자동 채택 금지 |
| 지역 음수대·벤치 | 지역별 보조 | 일부 공식 개별 파일 존재 | 전국 골격 없음, 파일별 주기·좌표·이용조건 상이 |

## 11. 프로토타입 전달 규칙

- 상세 화면에는 `출처`, `마지막 갱신/확인`, `높음/보통/낮음`, 등급 이유, 미확인 조건을 함께 표시한다.
- 결과 상단에는 출처 개수보다 `가장 오래된 유효 정보`, `정보가 없는 조건`, `확인 범위`를 우선 표시한다.
- 예시 장소나 예시 등급을 사용하면 `예시 데이터`라고 명시한다.
- `높음`이어도 `실제 운영 여부와 현장 상태는 출발 전 다시 확인하세요`를 유지한다.
- `정보 없음`은 시설 부재가 아니며, 자료가 없다는 이유로 가짜 장소·운영시간·갱신일을 채우지 않는다.

## 12. 미해결 입력

- 무더위쉼터 API 전량 프로파일링 완료: 60,672건, `reports/heat-shelter-profile.md`
- 화장실 지오코더 후보의 저장·상업 이용·재배포 조건과 실제 정확도·비용
- 표본 지역과 현장/원문 검수 승인
- 각 원출처의 개별 이용허락범위 보존 방식
- 두 번의 실제 수집 실행 후 관찰한 배포주기와 실패 패턴
- 실제 오안내 사례를 기준으로 한 5축 점수 구간 재보정
