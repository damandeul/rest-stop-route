# 쉬어갈지도 실데이터 생활서비스 디자인·검증 보고서

- 최종 검수 시각: `2026-07-30T05:32:34.550Z`
- 상태: 실데이터 UI·API·로컬 프로덕션 빌드 검증 완료, 운영 재배포 전

## 1. 현재 제품 흐름

쉬어갈지도는 예시 경로와 가짜 보행시간을 제거하고, 행정안전부 공식 무더위쉼터를 실제로 검색하는 생활서비스로 전환했다.

1. 사용자가 `현재 위치로 찾기`를 명시적으로 누르거나 주소·쉼터명을 직접 입력한다.
2. 검색 반경 `1·2·5·10km`와 냉방 자료 조건을 선택한다.
3. 공식 등록 쉼터를 직선거리순으로 확인한다.
4. 쉼터별 주소, 냉방기·선풍기 수량, 이용가능인원, 공개 운영시간, 자료 수정일을 확인한다.
5. 지도에서 공개 쉼터 위치를 열거나 보호자에게 쉼터 정보를 공유한다.

## 2. 데이터와 표현 원칙

- 출처: 행정안전부 재난안전데이터공유플랫폼
- 공식 검색 인덱스: `60,672건`
- 좌표 보유: `60,672건`
- 이용허락범위: 제한 없음
- 임의 장소·예시 경로·가짜 보행시간: 사용하지 않음
- 모든 결과는 `information_insufficient`로 표시한다.
- 공식 등록은 당일 개방, 좌석, 냉방기 작동, 즉시 출입 가능성이나 이동 안전을 보장하지 않는다고 명시한다.
- 공개 원문 링크, 원본 ID, 자료 수정일과 운영 제한 문구를 함께 제공한다.

## 3. 피움 디자인 방향

- 보라색 SaaS 그라데이션과 글래스모피즘을 사용하지 않았다.
- 웜그레이 종이 배경, 공공 안내판 계열 청록, 폭염 주의용 주황을 사용했다.
- 과도한 둥근 카드와 그림자를 줄이고 문서형 정보 위계를 적용했다.
- 색상뿐 아니라 제목, 굵은 선, 배지, 설명문으로 상태를 중복 표현했다.
- 모바일에서 현재 위치와 직접 입력을 경쟁시키지 않고 두 개의 명확한 선택 흐름으로 분리했다.
- 출처·제한 사유는 `<details>`로 확인할 수 있게 하고 모든 링크·버튼·요약의 터치 높이를 최소 44px로 유지했다.
- `prefers-reduced-motion`에서 불필요한 모션을 제거했다.

## 4. 위치정보와 공유 경계

- 위치정보는 페이지 로드 시 자동 요청하지 않는다.
- 사용자가 `현재 위치로 찾기`를 눌렀을 때만 브라우저 Geolocation을 호출한다.
- 권한 거부·시간 초과·위치 확인 실패 후에도 직접 입력 흐름과 포커스를 유지한다.
- 늦게 도착한 위치 응답은 사용자의 직접 선택을 덮어쓰지 않는다.
- 사용자 좌표는 브라우저 메모리에만 두며 localStorage·sessionStorage·분석 도구에 저장하지 않는다.
- 주변 조회는 POST body로 전송하고 URL과 API 응답에는 사용자 좌표를 넣지 않는다.
- 공유문에는 선택한 공식 쉼터명·주소·자료 제한·출처와 앱 origin만 포함하며 사용자 위치에서 파생된 거리도 제외한다.

## 5. 자동 검증 결과

| Gate | 결과 | 증거 |
| --- | --- | --- |
| Python 데이터·인덱스 테스트 | PASS | `6/6` |
| Playwright API·UI·보안 회귀 | PASS | `31/31` |
| 한국어 IME | PASS | 조합 중 DOM 유지, 완료 후 실제 검색 결과 표시 |
| 위치 실패 대안 | PASS | 권한 거부, 코드 2·3·99, 잘못된 좌표 |
| 위치 경쟁 상태 | PASS | 늦은 응답이 직접 선택·후속 위치 요청 버튼을 덮어쓰지 않음 |
| 잘못된 API 입력 | PASS | 빈값·null·배열·불리언·숫자 문자열·범위 밖 값을 400으로 거부 |
| 정적 파일 경계 | PASS | UI 파일만 공개, 서버·테스트·SQLite·라이브러리 404 |
| 의존성 감사 | PASS | 루트·프로토타입 `npm audit` 취약점 0건 |
| Vercel 로컬 프로덕션 빌드 | PASS | 함수·SQLite·Linux 네이티브 모듈 포함 |
| SQLite 배포 무결성 | PASS | 빌드 시 스키마·RTree·JSON·출처 검증, 런타임 SHA-256 재검증 |

## 6. 최신 디자인 QA

브라우저 QA 원본:

- `prototype/artifacts/design-qa.json`
- 재실행 스크립트: `prototype/scripts/design-qa.mjs`

검사 범위:

- viewport: `390×844`, `768×900`, `1024×900`, `1440×900`
- 화면: 검색, 공식 결과, 출처 상세 펼침
- 일반 화면: `12개`
- 200% 확대 동등 조건(`195 CSS px`): 검색·결과·출처 상세 `3개`
- 총 화면: `15개`
- 가로 오버플로: `0`
- 44px 미만 터치 대상: `0`
- 콘솔 오류: `0`
- 페이지 오류: `0`
- 실패 요청: `0`
- 200% 확대 3화면 가로 오버플로: `0`

390px 스크린샷:

- `prototype/artifacts/01-input-390.png`
- `prototype/artifacts/02-result-390.png`
- `prototype/artifacts/03-detail-390.png`

## 7. Vercel 번들 확인

`vercel build --prod` 산출물에서 다음을 확인했다.

- 정적 UI: `index.html`, `styles.css`, `app.js`
- 함수: `api/shelters`
- 검색 인덱스: 약 `33MB`
- 검색 인덱스 SHA-256 사이드카 포함
- `better-sqlite3` Linux 네이티브 바이너리 포함
- 함수 번들 전체: 약 `49MB`
- 빌드된 함수 직접 실행 결과:
  - health `200`, 공식 데이터 `60,672건`
  - `휴서울이동노동자북창쉼터` 실제 검색 `200`
  - 결과 상태 `information_insufficient`
  - DB 해시 불일치 시 `index_missing`으로 fail-closed

## 8. 남은 경계

- 헤드리스 Chromium 검증이며 실제 iOS·Android 기기 키보드·화면 회전은 아직 별도 실기기 검증하지 않았다.
- JavaScript 비활성 환경의 핵심 검색은 지원하지 않는다.
- 지도 링크는 외부 Google Maps를 사용하며 사용자가 누를 때 공개 쉼터 좌표만 전달한다.
- 운영 배포 후 공개 URL에서 API·직접 입력·결과·정적 파일 비노출을 다시 확인해야 한다.
