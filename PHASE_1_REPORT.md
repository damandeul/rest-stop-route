# 쉬어갈지도 Phase 1 독립 QA·최종 통합 보고서

- 판정 시각: 2026-07-27 15:12 KST
- 검토 범위: Phase 1 상위 문서 7개, 클릭 프로토타입, 공식 원출처·이름 조사, 안전·개인정보, 3화면 요구사항, 390px 모바일, 자동 테스트
- 최종 판정: **내부 클릭 프로토타입 Phase 1 통과(수정 후)**
- 승인 경계: 이 판정은 내부 프로토타입에 한정한다. 실제 전국 데이터 연결, 외부 사용자 테스트, 공개 배포, API 신청·비용 지출, 출시명 확정은 통과 판정에 포함하지 않는다.

## 1. 대표에게 먼저 보여줄 결론

1. **Phase 1 내부 검수용 산출물은 사용할 수 있다.** 요구한 3화면, 4개 결과 상태, 안전·개인정보 문구, 자료 범위·출처·갱신일·신뢰 수준, 공유 미리보기가 구현돼 있다.
2. 독립 QA에서 긍정 결과를 과장할 수 있는 **중요 논리 결함 2건**을 발견해 수정했다.
   - 서울 연결 표본에서 3분을 골라도 4분·5분 구간을 `조건에 맞는 후보가 이어짐`으로 표시하던 문제
   - 사용자가 `그늘`처럼 자료에서 확인되지 않은 조건을 골라도 긍정 연결로 표시하던 문제
3. 결과와 상세의 구간시간 불일치, 대안 선택 라디오의 44px 터치 영역 미달, 잘못 인코딩된 URL 한 건으로 로컬 서버가 종료되는 문제도 함께 수정했다.
4. 수정 후 후속 독립 리뷰에서 `정보 부족` 우선순위와 하드코딩된 시간 임계값을 추가 지적받아 보완했다. 최종적으로 원 작업경로와 독립 클린 복사본에서 각각 **15/15 테스트 통과**, `npm audit` **취약점 0건**, 3화면 브라우저 콘솔 **메시지 0·오류 0**을 확인했다.
5. 다만 실제 출시 가능성을 뜻하지 않는다. 전국 완전 커버리지는 아직 검증되지 않았고, 무더위쉼터 API 전량 수집·결측률, 화장실 지오코딩, 물·벤치 지역 커버리지, 표본 경로 현장 일치율은 미확인이다.
6. 이름 `쉬어갈지도`는 내부 사용을 계속할 수 있으나 출시명 확정은 보류해야 한다. 앱스토어 정확명 부재와 KIPRIS 기본검색 결과는 상표 사용 가능 판정이 아니다.

## 2. 검토한 산출물

- `DECISION.md`
- `PHASE_1_BRIEF.md`
- `DATA_AND_NAME_AUDIT.md`
- `DATA_TRUST_MODEL.md`
- `MVP_SPEC.md`
- `UX_STORYBOARD.md`
- `prototype/README.md`
- `prototype/index.html`, `styles.css`, `app.js`, `server.mjs`
- `prototype/tests/prototype.spec.js`, `tests/screenshot.spec.js`
- `prototype/artifacts/01-input-390.png`
- `prototype/artifacts/02-result-390.png`
- `prototype/artifacts/03-detail-390.png`

문서 간 핵심 결정은 일치한다. 전국 장소 검색과 전국 휴식점 완전 커버리지를 분리하고, `휴식점 부족`과 `정보 부족`을 구분하며, 자료가 없을 때 시설 부재를 단정하지 않는다.

## 3. 공식 데이터·이름 조사 독립 재확인

### 3.1 공식 데이터 원출처

2026-07-27에 아래 원문을 다시 요청했고 모두 HTTP 200과 공식 제목을 확인했다.

- 전국무더위쉼터표준데이터: https://www.data.go.kr/data/15013199/standard.do
  - 포털의 `갱신주기 수시`, Open API `업데이트 주기 실시간`, 무료·이용허락범위 제한 없음 표기를 재확인했다.
- 행정안전부 무더위쉼터: https://www.safetydata.go.kr/disaster-data/view?dataSn=1338
  - 원문 접근 가능성을 확인했다. 다만 페이지의 일부 API 필드는 동적 렌더링이라 이번 텍스트 추출만으로 `/V2/api/DSSP-IF-10942`, `MODF_TIME`, 1일 갱신 문구를 다시 증명하지는 못했다.
- 전국그늘막쉼터표준데이터: https://www.data.go.kr/data/15129447/standard.do
  - 연간 갱신, 위도·경도 필드를 확인했다. 제공기관 86개 주장은 페이지 값과 맞지만 `86`이 다른 HTML에도 등장하므로 출시 전 구조화 추출로 다시 고정해야 한다.
- 전국도시공원정보표준데이터: https://www.data.go.kr/data/15012890/standard.do
  - 제공기관 234개, 연간 갱신, 위도·경도 필드를 재확인했다.
- 전국공중화장실표준데이터: https://www.data.go.kr/data/15012892/standard.do
  - 2025년 2월부터 WGS84 위·경도 제공 중단, 매일 갱신·2일 전 기준 현행화 표기를 재확인했다.
  - 감사 문서의 `52,255행`은 현재 페이지 텍스트 추출에서 독립 재확인하지 못했다. 이 수치는 시점 변동 가능 값으로 보고 실제 파일 다운로드 후 행 수를 다시 계산해야 한다.
- 전국공공시설개방정보표준데이터: https://www.data.go.kr/data/15013117/standard.do
  - 제공기관 252개, 분기 갱신, 유료사용·신청·위도·경도 필드를 재확인했다. 냉방 실내 쉼터로 자동 채택하지 않는 문서 판단은 타당하다.

판정: 데이터 감사의 구조적 결론은 유지한다. 단, API 키 없이 전량 응답·행 수·결측률·17개 시도 실제 커버리지를 검증하지 않았으므로 `전국 데이터 확보 완료` 또는 `전국에서 연결 가능`이라고 표현해서는 안 된다.

### 3.2 앱·웹·상표 이름

- Apple 공식 Search API를 KR/US에서 `쉬어갈지도`, software, 최대 30건으로 재조회했다.
  - KR 결과 0건, US 결과 0건, 정확명 0건
  - `쉼터GO`(ID 6783315915), `그늘길`(6791532659), `그늘 맵`(6773795630), `그늘로: 뚜벅의 여름길`(6791194669)은 재확인했다.
- Google Play KR 웹의 `쉬어갈지도` 검색 첫 화면을 직접 확인했다.
  - 정확명 앱은 보이지 않았고 지도·걷기·`맛집갈지도` 등이 노출됐다.
  - 검색 개인화·지역·인덱스 한계 때문에 미존재 판정으로 쓰지 않는다.
- KIPRIS 공개 통합검색을 직접 다시 실행했다.
  - 검색어 `쉬어갈지도`의 상표 탭은 국내 **88건**을 표시했다.
  - 검색 결과는 `쉬어가는` 등 형태소 유사 결과를 폭넓게 포함하므로 88건을 직접 충돌 건수로 해석할 수 없다.
  - 정확명·띄어쓰기 변형·제9/42류·유사군 상세검색과 필요 시 전문가 검토 전에는 출시명 사용 가능 판정을 내리지 않는다.

판정: `정확명 앱을 확인 범위에서 발견하지 못했다`는 제한적 서술은 유지 가능하다. `이름 중복 없음`, `상표 등록 가능`, `법적 사용 가능`은 금지한다.

## 4. 독립 QA에서 발견하고 수정한 문제

### QA-01 — 선택 시간보다 긴 구간을 긍정 연결로 표시

- 심각도: 중요
- 재현: 서울시청 → 서울도서관, 3분, 확인된 조건 선택
- 기존 동작: 구간은 4분·5분인데 최상위가 `조건에 맞는 후보가 이어짐`
- 위험: 사용자의 핵심 보행 한도를 위반하며 안전 판단을 잘못 유도할 수 있음
- 수정: 하드코딩된 `3분/5분` 분기 대신 실제 `segmentTimes()`의 모든 구간이 선택 시간 이내인지 검사
- 회귀 테스트: `연결 표본도 선택 시간이 실제 구간보다 짧으면 휴식점 부족이다`

### QA-02 — 확인되지 않은 선택 조건으로 긍정 연결

- 심각도: 중요
- 재현: 서울시청 → 서울도서관, 5분, `그늘` 선택
- 기존 동작: 후보 카드에는 그늘이 `확인되지 않음`인데 최상위는 긍정 연결
- 위험: `unknown`을 `true`로 사용하지 않는다는 데이터 신뢰 모델의 하드 게이트 위반
- 수정: 선택 조건 중 후보의 확인된 조건에 없는 값이 하나라도 있으면 목적지 기본 시나리오가 CONNECTED/REST_GAP/ROUTE_ERROR인지와 관계없이 먼저 `정보 부족`
- 회귀 테스트: `선택 조건이 후보 자료에서 미확인이면 긍정 연결 대신 정보 부족이다`, `미확인 조건은 목적지의 휴식점 부족 시나리오보다 정보 부족을 우선한다`

### QA-03 — 결과와 상세의 구간시간 불일치

- 심각도: 보통
- 재현: 부산역, 3분 결과에서 구간 1은 8분·구간 2는 2분인데 상세는 4분·5분으로 표시
- 위험: 같은 계획 안에서 판단 근거가 바뀜
- 수정: 결과와 상세가 같은 `segmentTimes()`를 사용
- 회귀 테스트: `휴식점 부족 상세의 앞뒤 시간은 결과 카드와 일치한다`

### QA-04 — 대안 선택 터치 영역 미달

- 심각도: 보통
- 재현: 휴식점 부족 → 다른 이동 방법 살펴보기
- 기존 동작: 라디오 선택 행이 44px 미만
- 수정: 대화상자 선택 행 최소 44px, 라디오 20px로 확대
- 회귀 테스트: 네 개 선택 행의 높이 모두 44px 이상 확인

### QA-05 — 잘못 인코딩된 URL로 로컬 서버 종료

- 심각도: 보통
- 재현: `/%E0`처럼 올바르지 않은 퍼센트 인코딩 URL 요청
- 기존 동작: `decodeURIComponent()`의 `URIError`가 요청 처리 바깥으로 전파되어 Node 서버 종료
- 위험: 내부 검수 중 단일 비정상 요청으로 프로토타입을 사용할 수 없게 됨
- 수정: URL 디코딩 실패를 400 `Bad request`로 반환하고 서버를 계속 실행
- 회귀 테스트: 잘못 인코딩된 URL이 400이고 직후 `/` 요청이 200인지 확인

수정 파일:

- `prototype/app.js`
- `prototype/styles.css`
- `prototype/server.mjs`
- `prototype/tests/prototype.spec.js`
- `prototype/tests/screenshot.spec.js`
- `prototype/README.md`

## 5. 요구사항 추적 결과

| 요구 | 결과 | 근거 |
|---|---|---|
| 3화면 | 통과 | 입력 → 연결 결과 → 후보 상세 직접 실행 |
| 출발지·목적지·3/5/10분·5조건 | 통과 | 자동 테스트 및 브라우저 접근성 트리 |
| 4개 최상위 상태 | 통과 | CONNECTED/REST_GAP/DATA_GAP/ROUTE_ERROR 명시 렌더링 |
| 정보 부족 우선·임의 추정 금지 | 통과 | DATA_GAP·ROUTE_ERROR 테스트, QA-01/02 수정 |
| 출처·갱신일·자료 신뢰 수준·이유 | 통과 | 결과·상세 화면 직접 확인 |
| 운영정보 없음 경고 | 통과 | 상세 직접 확인 |
| 안전·응급 문구 | 통과 | 3화면 직접 확인, 금지 표현 소스 검색 0건 |
| 건강정보·정밀 이력 비저장 | 통과(프로토타입) | JS에 local/session storage·fetch·XHR·beacon·WebSocket 없음 |
| 공유 전 포함 정보 고지·실제 전송 없음 | 통과 | 공유 미리보기 자동 테스트 |
| 390px 가로 오버플로 없음 | 통과 | 3화면 DOM 검사 0, 시각 검수 통과 |
| 터치 영역 | 통과(수정 후) | 주요 컨트롤 44/48/52px, 대안 선택 44px 회귀 테스트 |
| 콘솔 오류 없음 | 통과 | 입력·결과·상세 콘솔 메시지 0, JS 오류 0 |
| 외부 전환 확인 | 통과 | 지도·원문 확인 대화상자, 실제 외부 전환 없음 |
| 전국 완전 커버 주장 금지 | 통과 | 데모·예시 데이터·내부 표본·한계 명시 |

## 6. 실행 검증 기록

### 원 작업경로

```text
npm test
Running 15 tests using 2 workers
15 passed (4.4s)

npm audit --audit-level=low
found 0 vulnerabilities
```

### 독립 클린 복사본

`node_modules`와 기존 테스트 결과를 제외하고 `/tmp/rest-stop-phase1-qa`에 복사한 뒤 실행했다.

```text
npm ci
added 4 packages, and audited 5 packages
found 0 vulnerabilities

npm test
Running 15 tests using 2 workers
15 passed (4.4s)
```

### 추가 검사

- `node --check app.js`, `server.mjs`, `tests/prototype.spec.js`: 통과
- 로컬 서버: `127.0.0.1:4173`에서 HTTP 200, `Cache-Control: no-store`
- 경로 이탈 요청 `/%2e%2e/%2e%2e/TEAM.md`: HTTP 404
- 잘못 인코딩된 URL `/%E0`: HTTP 400, 직후 홈 HTTP 200로 서버 생존 확인
- 금지 표현 소스 검색: 0건
- 저장·외부 전송 API 소스 검색: 0건
- 브라우저 콘솔: 입력·결과·상세 각각 메시지 0, JS 오류 0
- 스크린샷:
  - 입력 390×1374
  - 결과 390×1754
  - 상세 390×1845
- 육안 검수: 텍스트 잘림·겹침·가로 넘침 없음. 상태는 색상 외에 배지·아이콘·제목·설명으로 전달됨.

## 7. 실행 방법과 경로 주의

권장 실행:

```bash
cd public-app-ideas/rest-stop-route/prototype
npm ci
npm run serve
```

브라우저: `http://127.0.0.1:4173`

테스트:

```bash
cd public-app-ideas/rest-stop-route/prototype
npm test
```

한글 작업경로에서 `npm --prefix <경로>`를 쓰면 macOS Unicode 정규화 차이로 Playwright가 중복 로드될 수 있다. README처럼 디렉터리로 직접 `cd`한 뒤 실행하면 이번 독립 검증에서 정상 통과했다.

## 8. 남은 제약·위험

1. 화면의 장소·주소·시간·갱신일은 모두 예시 데이터다. 실제 이동 판단에 사용하면 안 된다.
2. 실제 전국 주소 검색, 보행 경로, 공공데이터 API, 공유 링크는 구현하지 않았다.
3. 무더위쉼터 API 이용신청·전량 프로파일링이 없어 전체 행 수와 결측률을 모른다.
4. 화장실은 좌표가 없어 지오코더 정확도·저장·상업 이용·재배포 조건 검토가 필요하다.
5. 물·벤치는 전국 표준 골격이 확인되지 않았다.
6. 그늘막 운영·계절 상태, 공원 중심점과 실제 출입구·벤치 위치 오차는 해결되지 않았다.
7. 상표 기본검색은 법률 검토가 아니다.
8. 외부 사용성 수치(80%, 60% 등)는 아직 참여자를 모집하지 않았으므로 `검증 미완료`다.
9. 200% 텍스트 확대는 이번 자동화에 전용 회귀 테스트가 없다. 390px 기본 크기·DOM 오버플로·전체 페이지 육안 검수는 통과했지만, 실제 브라우저/OS 확대 조합은 다음 접근성 점검에서 별도로 수행해야 한다.

## 9. 대표 결정이 필요한 다음 단계

Phase 1은 내부 프로토타입 기준 통과로 종료하고, 실제 제작 전 아래를 결정해야 한다.

1. 대표 검수용 표본 지역과 30개 경로 구성 승인
2. 무더위쉼터 API 이용신청 및 전량 데이터 프로파일링 승인 여부
3. 화장실 지오코더 후보의 라이선스·정확도·비용 조사 범위
4. 외부 사용자 5명 이상 테스트 승인 여부
5. 출시명 확정 전 KIPRIS 상세검색·전문가 검토 수준
6. 실제 공유 링크 구현 여부와 만료 정책

추천 순서: **데이터 전량 프로파일링 → 표본 경로 30개 → 내부 실제값 프로토타입 → 대표 승인 후 외부 사용자 테스트**. 전국 공개 배포는 이 네 단계를 통과한 뒤 판단한다.

## 10. 핵심 아티팩트 경로

- 최종 보고서: `public-app-ideas/rest-stop-route/PHASE_1_REPORT.md`
- 실행 브리프: `public-app-ideas/rest-stop-route/PHASE_1_BRIEF.md`
- 데이터·이름 감사: `public-app-ideas/rest-stop-route/DATA_AND_NAME_AUDIT.md`
- 데이터 신뢰 모델: `public-app-ideas/rest-stop-route/DATA_TRUST_MODEL.md`
- MVP 명세: `public-app-ideas/rest-stop-route/MVP_SPEC.md`
- UX 스토리보드: `public-app-ideas/rest-stop-route/UX_STORYBOARD.md`
- 프로토타입: `public-app-ideas/rest-stop-route/prototype/`
- 실행 안내: `public-app-ideas/rest-stop-route/prototype/README.md`
- 390px 스크린샷: `public-app-ideas/rest-stop-route/prototype/artifacts/`
